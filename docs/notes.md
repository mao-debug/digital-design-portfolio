Mux or Multiplexer allows for multiple signals to be filtered down to one.
In the event you have 4 signals that follow the same path but at different times, use a mux to ensure only one signal makes it through at a time
Mux are also scaleable and you can build a 4-1 using 3 2-1
